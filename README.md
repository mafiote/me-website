
## Development server

Run `ng serve --host 0.0.0.0 --port 16010 --disable-host-check` or `npm start` for a dev server. Navigate to `http://localhost:16010/` or `http://ip-address:16010/` access from lan . The app will automatically reload if you change any of the source files.
